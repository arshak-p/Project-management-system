[phases.setup]
nixPkgs = ["...", "nodejs"]

[phases.install]
cmds = ["pip install -r requirements.txt", "cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "daphne -b 0.0.0.0 -p $PORT colour_parrot.asgi:application"
